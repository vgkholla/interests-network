mutation createGardenPlant($name: String!, $gardenId: String!, $plantId: String!) {
    createGardenPlant(input: { gardenPlant: {
        name: $name,
        gardenId: $gardenId,
        plantId: $plantId
    }}) {
        id
    }
}
