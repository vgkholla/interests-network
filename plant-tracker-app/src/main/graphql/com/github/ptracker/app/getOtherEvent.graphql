query getOtherEvent($id: String!) {
    getOtherEvent(input: { id: $id }) {
        id
        description
        metadata {
            timestamp
            comment
            gardener {
                id
                firstName
                lastName
            }
        }
    }
}