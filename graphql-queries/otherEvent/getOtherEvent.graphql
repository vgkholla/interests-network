query getOtherEvent($id: String!) {
    getOtherEvent(input: { id: $id }) {
        description
        metadata {
            timestamp
            comment
            gardener {
                id
                firstname
                lastname
            }
        }
    }
}