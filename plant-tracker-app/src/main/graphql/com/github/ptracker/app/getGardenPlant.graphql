query getGardenPlant($id: String!) {
    getGardenPlant(input: { id: $id }) {
        id
        name
        plant {
            id
            name
        }
        fertilizationEvents {
            id
        }
        wateringEvents {
            id
        }
        otherEvents {
            id
        }
    }
}