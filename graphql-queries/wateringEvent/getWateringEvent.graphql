query getWateringEvent($id: String!) {
    getWateringEvent(input: { id: $id }) {
        quantityml
        metadata {
            timestamp
            comment
            gardener {
                id
                firstname
                lastname
            }
        }
    }
}